server:
  port: 8083

spring:
  application:
    name: media-service
  mongodb:
    uri: mongodb://root:password@localhost:27019/media-service-db?authSource=admin
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

cloudinary:
  cloud-name: "jjwxm4oa"
  api-key: "871669674861459"
  api-secret: "tARoCmvlEW9-lXKPA4nKr9M097E"